import { CreateFundingRoundDto } from './create-funding-round-dto';

describe('CreateFundingRoundDto', () => {
  it('should be defined', () => {
    expect(new CreateFundingRoundDto()).toBeDefined();
  });
});
