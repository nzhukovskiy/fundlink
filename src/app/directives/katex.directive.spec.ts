import { KatexDirective } from './katex.directive';

describe('KatexDirective', () => {
  it('should create an instance', () => {
    const directive = new KatexDirective();
    expect(directive).toBeTruthy();
  });
});
