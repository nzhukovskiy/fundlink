import { CreateRefreshTokenDto } from './create-refresh-token.dto';

describe('CreateRefreshTokenDto', () => {
  it('should be defined', () => {
    expect(new CreateRefreshTokenDto()).toBeDefined();
  });
});
