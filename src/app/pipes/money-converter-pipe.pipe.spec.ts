import { MoneyConverterPipePipe } from './money-converter-pipe.pipe';

describe('MoneyConverterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MoneyConverterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
